============
Installation
============

At the command line::

    $ easy_install django-querche

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-querche
    $ pip install django-querche
